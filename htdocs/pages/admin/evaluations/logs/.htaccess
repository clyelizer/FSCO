# Protection du dossier logs
# Bloquer complètement l'accès

Order Deny,Allow
Deny from all

# Ne pas lister le contenu
Options -Indexes

# Headers de sécurité
<IfModule mod_headers.c>
    Header always set X-Robots-Tag "noindex, nofollow, noarchive"
</IfModule>